import { expect } from 'chai';
import sinon from 'sinon';

import PropertySorter from '../../src/sorters/property_sorter';

describe('PropertySorter', () => {
  describe('_normalizeItem', () => {
    // let subject;
    // beforeEach(() => {
    //   subject = new PropertySorter();
    // });
    it('should return the PUSH_TO_* val instead of the item when the value is empty', () => {
      let subject = new PropertySorter();
      '';
    });
  });

  describe('_dealWithEmptyItems', () => {

  });
});
